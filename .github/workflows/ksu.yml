name: Ksunati

on:
  workflow_dispatch:
    inputs:
      KERNEL_REPO:
        description: 'Kernel Kaynak Linki (Git URL)'
        required: true
        default: 'https://github.com/oliplusmzr/android_kernel_samsung_a24'
      KERNEL_BRANCH:
        description: 'Branch (Dal) Adı'
        required: true
        default: 'OSRC'
      DEFCONFIG_NAME:
        description: 'Defconfig Dosya Adı'
        required: true
        default: 'a24_defconfig'
      ROOT_METHOD:
        description: 'Root Yöntemi Seç'
        required: true
        default: 'KernelSU-Next'
        type: choice
        options:
        - None
        - KernelSU
        - KernelSU-Next
        - APatch

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
    - name: 📦 Ortamı Hazırla
      run: |
        sudo apt-get update
        # BURASI GÜNCELLENDİ: gcc-aarch64 ve arm-linux eklendi
        sudo apt-get install -y git bc bison flex libssl-dev build-essential libncurses5-dev libncurses5 libelf-dev gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi
        
    - name: 📥 Kernel Kaynağını Çek
      run: |
        git clone --depth=1 -b ${{ inputs.KERNEL_BRANCH }} ${{ inputs.KERNEL_REPO }} android_kernel

    - name: 🛠️ Root Entegre Et
      working-directory: android_kernel
      run: |
        echo "Seçilen Yöntem: ${{ inputs.ROOT_METHOD }}"
        
        if [ "${{ inputs.ROOT_METHOD }}" == "KernelSU" ]; then
            curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
            echo "CONFIG_KSU=y" >> arch/arm64/configs/${{ inputs.DEFCONFIG_NAME }}
            
        elif [ "${{ inputs.ROOT_METHOD }}" == "KernelSU-Next" ]; then
            curl -LSs "https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh" | bash -
            echo "CONFIG_KSU=y" >> arch/arm64/configs/${{ inputs.DEFCONFIG_NAME }}
            
        elif [ "${{ inputs.ROOT_METHOD }}" == "APatch" ]; then
            git clone https://github.com/bmax121/APatch drivers/apatch
            echo "CONFIG_APATCH=y" >> arch/arm64/configs/${{ inputs.DEFCONFIG_NAME }}
        fi

    - name: 🔧 Toolchain (Proton Clang)
      run: |
        git clone --depth=1 https://github.com/kdrag0n/proton-clang clang

    - name: 🔥 Derlemeyi Başlat
      working-directory: android_kernel
      run: |
        export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
        export ARCH=arm64
        export SUBARCH=arm64
        export CROSS_COMPILE=aarch64-linux-gnu-
        export CC=clang
        export CLANG_TRIPLE=aarch64-linux-gnu-
        
        # Defconfig dosyasını yüklüyoruz
        make O=out ${{ inputs.DEFCONFIG_NAME }}
        
        # Derleme komutu
        make O=out -j$(nproc --all) \
            LLVM=1 \
            LLVM_IAS=1

    - name: 📤 Sonucu Paketle
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-${{ inputs.ROOT_METHOD }}
        path: |
          android_kernel/out/arch/arm64/boot/Image*
          android_kernel/out/arch/arm64/boot/*.dtb
          android_kernel/out/arch/arm64/boot/*.gz
          
