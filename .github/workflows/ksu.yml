name: Ksunati

on:
  workflow_dispatch:
    inputs:
      KERNEL_REPO:
        description: 'Kernel Kaynak Linki (Git URL)'
        required: true
        default: 'https://github.com/oliplusmzr/android_kernel_samsung_a24'
      KERNEL_BRANCH:
        description: 'Branch (Dal) Adı'
        required: true
        default: 'OSRC'
      DEFCONFIG_NAME:
        description: 'Defconfig Dosya Adı'
        required: true
        default: 'a24_defconfig'
      ROOT_METHOD:
        description: 'Root Yöntemi Seç'
        required: true
        default: 'KernelSU-Next'
        type: choice
        options:
        - None
        - KernelSU
        - KernelSU-Next
        - APatch

jobs:
  build:
    # 🔥 HIZLI MAKİNE: Ubuntu 22.04 (Sıra bekleme derdi yok)
    runs-on: ubuntu-22.04
    
    steps:
    - name: 📦 Ortamı Hazırla
      run: |
        sudo apt-get update
        sudo apt-get install -y git bc bison flex build-essential libncurses5-dev libncurses5 libelf-dev gcc-aarch64-linux-gnu gcc-
